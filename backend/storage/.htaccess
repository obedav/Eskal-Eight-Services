# Deny access to storage directory from web
# Only upload files should be accessible via proper endpoints

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
